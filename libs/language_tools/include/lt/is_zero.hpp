#ifndef LT_IS_ZERO_HPP
#define LT_IS_ZERO_HPP

namespace lt
{
    // ============================================================================================================== //

    bool is_zero(int const value);
    bool is_zero(long int const value);
    bool is_zero(float const value);
    bool is_zero(double const value);
    bool is_zero(long double const value);

    // ============================================================================================================== //

} // namespace lt

#endif // LT_IS_ZERO_HPP
